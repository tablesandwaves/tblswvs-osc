{
  "name": "tblswvs-osc",
  "version": "0.2.0",
  "description": "OSC communication library for the tblswvs ecosystem",
  "keywords": [
    "tblswvs",
    "osc"
  ],
  "license": "MIT",
  "author": "Tables & Waves",
  "type": "module",
  "files": [
    "build"
  ],
  "exports": {
    "types": "./build/index.d.ts",
    "import": "./build/index.js"
  },
  "scripts": {
    "test": "npm run compile && node --trace-warnings --test tests/**/*.test.js",
    "compile": "rm -rf build/* && tsc"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "~6.0.3"
  },
  "dependencies": {
    "osc-min": "^2.1.2"
  }
}
